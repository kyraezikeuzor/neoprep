import DashboardPageShell from "@/components/DashboardPageShell";
import Link from "next/link";
import PageHeader from "@/components/PageHeader";
import { listAdminRoadmapStudents } from "@/app/actions/bootcamp";
import { GenerateStudentSetButton } from "@/components/admin/RoadmapAdminControls";
export default async function AdminStudentsPage() { const students = await listAdminRoadmapStudents(); return <DashboardPageShell><PageHeader title="Students" description="Monitor Roadmap progress, accuracy, and missed questions." /><div className="mt-6 overflow-x-auto arc-card"><table className="w-full text-left text-sm"><thead><tr className="border-b border-arc-line text-[#747474]"><th className="p-4">Student</th><th className="p-4">Active set</th><th className="p-4">Progress</th><th className="p-4">Actions</th></tr></thead><tbody>{students.map((student) => <tr key={student.student_id} className="border-b border-arc-line last:border-0"><td className="p-4"><p>{student.full_name || "Unnamed student"}</p><p className="text-xs text-[#747474]">{student.email}</p></td><td className="p-4">{student.active_assignment || "No active set"}</td><td className="p-4">{student.total ? `${student.completed}/${student.total}` : "—"}</td><td className="flex gap-2 p-4"><Link href={`/admin/students/${student.student_id}`} className="arc-btn-secondary min-h-9 px-3 text-xs">View progress</Link><GenerateStudentSetButton studentId={student.student_id} /></td></tr>)}</tbody></table></div></DashboardPageShell>; }
