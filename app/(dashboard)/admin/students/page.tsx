import DashboardPageShell from "@/components/DashboardPageShell";
import PageHeader from "@/components/PageHeader";
import { listAdminRoadmapStudents } from "@/app/actions/bootcamp";
import { GenerateStudentSetButton } from "@/components/admin/RoadmapAdminControls";
export default async function AdminStudentsPage() { const students = await listAdminRoadmapStudents(); return <DashboardPageShell><PageHeader title="Students" description="Monitor Roadmap progress and generate the next adaptive Question Set." /><div className="mt-6 overflow-x-auto arc-card"><table className="w-full text-left text-sm"><thead><tr className="border-b border-arc-line text-[#747474]"><th className="p-4">Student</th><th className="p-4">Active set</th><th className="p-4">Progress</th><th className="p-4" /></tr></thead><tbody>{students.map((student) => <tr key={student.student_id} className="border-b border-arc-line last:border-0"><td className="p-4"><p>{student.full_name || "Unnamed student"}</p><p className="text-xs text-[#747474]">{student.email}</p></td><td className="p-4">{student.active_assignment || "No active set"}</td><td className="p-4">{student.total ? `${student.completed}/${student.total}` : "—"}</td><td className="p-4"><GenerateStudentSetButton studentId={student.student_id} /></td></tr>)}</tbody></table></div></DashboardPageShell>; }
